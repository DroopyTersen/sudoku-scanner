{
  "name": "sudoku",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/entry.ts --bundle --minify --sourcemap --outfile=public/bundle.js",
    "watch": "chokidar \"src/**/*\" -d 10 --initial -c \"npm run build\"",
    "serve": "cd public & live-server",
    "start": "concurrently \"npm run watch\" \"npm run serve\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar": "^3.4.3",
    "chokidar-cli": "^2.1.0",
    "concurrently": "^5.3.0",
    "esbuild": "^0.8.4",
    "live-server": "^1.2.1",
    "ngrok": "^3.3.0",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "preact": "^10.5.5"
  }
}